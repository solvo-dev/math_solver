[project]
name = "version-4"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "datasets>=2.13.0",
    "gradio==4.44.1",
    "httpx>=0.25.0",
    "joblib>=1.3.0",
    "mpmath>=1.3.0",
    "orjson>=3.9.0",
    "python-dotenv>=1.0.0",
    "rapidfuzz>=3.0.0",
    "scikit-learn>=1.3.0",
    "sentence-transformers>=2.2.2",
    "sympy>=1.12.0",
    "watchdog>=3.0.0",
]

[tool.setuptools.packages.find]
where = ["src"]
include = ["*"]

[project.scripts]
math-solver = "math_solver.main:main"
